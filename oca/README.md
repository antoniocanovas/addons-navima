## Directorio para añadir enlaces de módulos OCA

Para añadir módulos, situarse dentro del directorio oca y lanzar el siguiente comando:

`ln -sf ../../repositorio/modulo .`

Por ejemplo:

`ln -sf ../../l10n-spain/l10n_es_aeat .`
