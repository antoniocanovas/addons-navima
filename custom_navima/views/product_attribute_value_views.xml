<?xml version="1.0"?>
<odoo>

    <!-- CUSTOM TREE VIEW (default form not required): -->
    <record id="product_attribute_value_group_inh_tree" model="ir.ui.view">
        <field name="name">product.attribute.value.group.inh.tree</field>
        <field name="model">product.attribute.value</field>
        <field name="inherit_id" ref="product.product_attribute_value_list"/>
        <field name="type">tree</field>
        <field name="arch" type="xml">
            <field name="name" position="after">
                <field name="group_id"/>
            </field>
        </field>
    </record>

</odoo>